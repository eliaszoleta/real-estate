# Leave unset for local dev (uses CRA's proxy) and for a single unified Vercel
# multi-service deploy (uses vercel.json rewrites). Only set this if the backend
# is deployed on a separate origin, e.g. Railway.
# REACT_APP_API_BASE=https://your-backend.railway.app
