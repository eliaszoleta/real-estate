PORT=3001
FRONTEND_URL=https://your-frontend.vercel.app

# Optional: enables live per-address AVM estimates + comp photos via
# services/listingsApiService.js. Leave unset to run entirely on the free
# public-data engine. Verify the current API contract against RentCast's
# docs before relying on this in production — see that file's header comment.
# RENTCAST_API_KEY=
